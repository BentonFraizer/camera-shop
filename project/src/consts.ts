export enum AppRoute {
  Catalog = '/',
  Product = '/product/:id',
  Basket = '/basket',
}

export enum NameSpace {
  Data = 'DATA',
}

export enum APIRoute {
  Cameras = '/cameras',
  Promo = '/promo',
}
